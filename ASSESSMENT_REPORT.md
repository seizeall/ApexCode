# 考核要求符合性检查

检查依据：`推免考核题目学生版.pdf`（2 页）。检查时间：2026-09-01。

## 代码与功能

| 题目要求 | 结论 | 项目证据 |
|---|---|---|
| 独立设计 coding agent | 符合 | `app/agent/service.py` 自写循环，无 Agent 框架 |
| 与大模型交互 | 符合 | `app/model/client.py` 实现 OpenAI/Anthropic 兼容 HTTP 客户端 |
| 自主读写文件、执行命令 | 符合 | `app/tools/registry.py` 提供 6 种本地工具 |
| 不依赖托管执行/文件工具 | 符合 | 文件与命令均在本机工作区执行 |
| 对话历史与上下文管理 | 符合 | 会话持久化、历史恢复、字符预算裁剪 |
| 工具定义与本地执行 | 符合 | Pydantic 参数校验、路径约束、结果回传 |
| 模型输出解析 | 符合 | 统一解析文本、tool_calls、function_call、内容块 |
| 循环终止条件 | 符合 | 最大轮数、工具数、无工具调用、用户取消 |
| 错误处理 | 符合 | 网络重试、超时、异常事件、危险命令拦截 |
| 真实密钥不入库 | 符合 | `.env` 被忽略，仅提供空白 `.env.example` |
| 可运行产品 | 符合 | CLI、FastAPI 动态网站、SSE 事件、会话和上传 |
| Windows 一键使用 | 额外交付 | `ApexCode-Setup.exe` 安装版及便携 ZIP |

自动化验证：37 个测试全部通过；Python 编译检查通过；动态示例 JavaScript 语法通过；浏览器实测新增、完成、筛选、刷新持久化及 API 配置弹窗通过且无控制台错误；打包 exe 启动后首页与 `/api/config` 正常响应，并确认接口不回传密钥。

## 提交材料

当前还不能称为“完全提交合规”，原因不是代码功能，而是演示视频和最终表单仍需本人完成：

1. 录制 2 分钟以内、MP4、200 MB 以内的视频。可按 `DEMO_GUIDE.md` 录制。
2. 题目要求公开仓库保留完整提交历史。仓库已推送且无需凭据即可读取，提交时间与内容连续；不要压缩或改写历史，并准备在面试时解释设计决策。
3. 最终表单只上传“姓名.zip”，内容仅为 `README.txt` 和视频；不要把源码、真实 `.env` 或 API key 放进提交 ZIP。

README 已控制在 1000 个字符以内并包含 Git 仓库地址、运行方式和特色功能。截止时间为 2026-09-02 24:00（北京时间）。
